import React from 'react';
import Nav from './Nav';

const Header = props => {
    return (
        <header className='admin-header'>
            <Nav />
        </header>
    );
}
export default Header;