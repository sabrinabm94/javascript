import React from "react";
import Nav from "./Nav";

const Header = (props) => {
    return (
        <header id="header" className="header">
            <Nav />
        </header>
    );
};
export default Header;
