import { User } from '@models//user';

export class PartnerIdealization extends User {
    ageRange: Object = {
        inferiorLimit: Number,
        upperLimit: Number
    };
    heightRange: Object = {
        inferiorLimit: Number,
        upperLimit: Number
    };
}























