import React from 'react';
import Nav from './Nav';

const Header = props => {
    return (
        <header className='header' id='header'>
            <Nav />
        </header>
    );
}
export default Header;
