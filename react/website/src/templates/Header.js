import React from "react";
import Nav from "./Nav";

const Header = (props) => {
    return (
        <header className="main-header" id="header">
            <Nav />
        </header>
    );
};
export default Header;
